# Workspace-level .gitignore

# Dialog exports (may contain secrets)
dialog/*.md
dialog/*.json
dialog/*.txt
dialog/*.html

# Framework internal files
.claude/.last_session
.claude/.framework-config
.claude/logs/

# Environment variables
.env
.env.*
!.env.example
!.env.template

# IDE and system files
.DS_Store
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Note: Project-specific .gitignore files handle their own artifacts
# (node_modules, dist, .next, etc. are in project directories)
emcd-agent/
